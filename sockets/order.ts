import { Server } from "socket.io"
// import { addPurchase_, updatePurchase_ } from "../midCondroller/Purchase.js"
// import { AddOrder_, getInationalOrderBatches_ } from "../midCondroller/order.js";

export const orderSoket = async (socket: Server) => {

    // socket.on("add_order", async ({form, purchasesId }) => {
    //     console.log({form, purchasesId });
        
    //     await AddOrder_(form, purchasesId, socket);
    // })

    // socket.on("get_inational_order_batches", async (limit) => {
    //     await getInationalOrderBatches_(limit, socket);
    // })

    

    // socket.on("update_purchase", async (Purchase) => {
    //     await updatePurchase_(Purchase, socket);
    // })



}